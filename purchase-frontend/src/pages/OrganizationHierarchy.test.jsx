/* eslint-disable testing-library/no-node-access */
import React from 'react';
import {render,screen,waitFor,within} from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import OrganizationHierarchy from './OrganizationHierarchy';
import * as api from '../api/organization';
import {useAuth} from '../hooks/useAuth';
jest.mock('../api/organization');
jest.mock('../hooks/useAuth');
const root={id:1,name:'Institute',unit_type:'INSTITUTE',is_active:true,children:[{id:2,name:'Finance',code:'FIN',unit_type:'DEPARTMENT',parent_unit_id:1,is_active:true,classification:'Clinical',children:[{id:3,name:'Payroll',unit_type:'SECTION',parent_unit_id:2,is_active:true,children:[]}]}]};
const detail={...root.children[0],parent_name:'Institute',path:['Institute','Finance'],department_name:'Finance legacy',positions:[{id:9,position_type:'UNIT_HEAD',position_name:'Director',user_id:7,user_name:'Ada',is_unit_head:true,is_active:true}],unitHead:{user_name:'Ada'},executiveOwner:{userName:'Grace'}};
const setup=(manager=true)=>{useAuth.mockReturnValue({user:{permissions:manager?['organization.manage']:[]}});api.getOrganizationTree.mockResolvedValue([root]);api.getOrganizationOptions.mockResolvedValue({departments:[{id:11,name:'Legacy Finance'}],sections:[{id:12,name:'Payroll',department_name:'Legacy Finance'}],users:[{id:7,name:'Ada',email:'a@x.test'}]});api.getOrganizationUnit.mockResolvedValue(detail);api.getOrganizationPositions.mockResolvedValue(detail.positions);return render(<OrganizationHierarchy/>)};
beforeEach(()=>jest.clearAllMocks());
test('renders loading then hierarchy and switches views',async()=>{let release;api.getOrganizationTree.mockReturnValue(new Promise(r=>release=r));useAuth.mockReturnValue({user:{permissions:[]}});api.getOrganizationOptions.mockResolvedValue({departments:[],sections:[],users:[]});render(<OrganizationHierarchy/>);expect(screen.getByText(/Loading organization/)).toBeInTheDocument();release([root]);expect(await screen.findByText('Finance')).toBeInTheDocument();await userEvent.click(screen.getByRole('button',{name:'Table view'}));expect(screen.getByRole('columnheader',{name:'Parent'})).toBeInTheDocument()});
test('shows API failure',async()=>{useAuth.mockReturnValue({user:{permissions:[]}});api.getOrganizationTree.mockRejectedValue({response:{data:{message:'Network unavailable'}}});api.getOrganizationOptions.mockResolvedValue({departments:[],sections:[],users:[]});render(<OrganizationHierarchy/>);expect(await screen.findByRole('alert')).toHaveTextContent('Network unavailable')});
test('search preserves ancestor path and expand collapse works',async()=>{setup(false);await screen.findByText('Payroll');await userEvent.type(screen.getByLabelText('Search units'),'payroll');expect(screen.getByText('Institute')).toBeInTheDocument();expect(screen.getByText('Finance')).toBeInTheDocument();await userEvent.click(screen.getByRole('button',{name:'Collapse All'}));expect(screen.queryByText('Payroll')).not.toBeInTheDocument();await userEvent.click(screen.getByRole('button',{name:'Expand All'}));expect(screen.getByText('Payroll')).toBeInTheDocument()});
test('permissions gate mutations',async()=>{const {unmount}=setup(false);await screen.findByText('Finance');expect(screen.queryByRole('button',{name:/Add Unit/})).not.toBeInTheDocument();unmount();setup(true);expect(await screen.findByRole('button',{name:/Add Unit/})).toBeInTheDocument()});
test('adds department using scoped identity and selects result',async()=>{setup();api.createOrganizationUnit.mockResolvedValue({id:4});await screen.findByText('Finance');await userEvent.click(screen.getByRole('button',{name:/Add Unit/}));const d=screen.getByRole('dialog');await userEvent.type(within(d).getByLabelText('Name'),'Operations');await userEvent.selectOptions(within(d).getByLabelText('Unit type'),'DEPARTMENT');await userEvent.selectOptions(within(d).getByLabelText('Parent'),'1');await userEvent.selectOptions(within(d).getByLabelText('Department'),'11');await userEvent.click(within(d).getByRole('button',{name:'Save'}));await waitFor(()=>expect(api.createOrganizationUnit).toHaveBeenCalledWith(expect.objectContaining({name:'Operations',unitType:'DEPARTMENT',parentUnitId:'1',departmentId:'11'})))});
test('add reports validation/API errors',async()=>{setup();api.createOrganizationUnit.mockRejectedValue({response:{data:{message:'Code already exists'}}});await screen.findByText('Finance');await userEvent.click(screen.getByRole('button',{name:/Add Unit/}));const d=screen.getByRole('dialog');await userEvent.click(within(d).getByRole('button',{name:'Save'}));expect(within(d).getByRole('alert')).toHaveTextContent('Name is required');await userEvent.type(within(d).getByLabelText('Name'),'Duplicate');await userEvent.click(within(d).getByRole('button',{name:'Save'}));expect(await within(d).findByText('Code already exists')).toBeInTheDocument()});
test('edits only safe metadata',async()=>{setup();api.updateOrganizationUnit.mockResolvedValue(detail);await userEvent.click(await screen.findByText('Finance'));await screen.findByText('Full path');await userEvent.click(screen.getByRole('button',{name:'Edit'}));const d=screen.getByRole('dialog');expect(within(d).queryByLabelText('Parent')).not.toBeInTheDocument();await userEvent.clear(within(d).getByLabelText('Name'));await userEvent.type(within(d).getByLabelText('Name'),'Finance Office');await userEvent.click(within(d).getByRole('button',{name:'Save'}));await waitFor(()=>expect(api.updateOrganizationUnit).toHaveBeenCalledWith(2,expect.objectContaining({name:'Finance Office'})))});
test('moves with confirmation and displays backend cycle error',async()=>{setup();api.moveOrganizationUnit.mockRejectedValueOnce({response:{data:{message:'Move would create a circular hierarchy'}}});await userEvent.click(await screen.findByText('Finance'));await screen.findByText('Full path');await userEvent.click(screen.getByRole('button',{name:'Move'}));const d=screen.getByRole('dialog');expect(within(d).getByText(/Historical approvals are not changed/)).toBeInTheDocument();await userEvent.selectOptions(within(d).getByLabelText('Target parent'),'3');await userEvent.click(within(d).getByText('I confirm this move'));await userEvent.click(within(d).getByRole('button',{name:'Move unit'}));expect(await within(d).findByText(/circular hierarchy/)).toBeInTheDocument()});
test('archives after confirmation and reports errors',async()=>{setup();window.confirm=jest.fn(()=>true);api.archiveOrganizationUnit.mockResolvedValue({});await userEvent.click(await screen.findByText('Finance'));await screen.findByText('Full path');await userEvent.click(screen.getByRole('button',{name:'Archive'}));await waitFor(()=>expect(api.archiveOrganizationUnit).toHaveBeenCalledWith(2))});
test('positions render statuses, add, edit, archive, and duplicate authority error',async()=>{setup();window.confirm=jest.fn(()=>true);api.getOrganizationPositions.mockResolvedValue([{id:1,position_type:'CUSTOM',position_name:'Future',user_id:7,user_name:'Ada',is_active:true,effective_from:'2999-01-01'},{id:2,position_type:'CUSTOM',position_name:'Expired',user_id:7,is_active:true,effective_to:'2000-01-01'},{id:3,position_type:'CUSTOM',position_name:'Vacant',user_id:null,is_active:true}]);api.createOrganizationPosition.mockRejectedValue({response:{status:409,data:{message:'unique authority'}}});api.archiveOrganizationPosition.mockResolvedValue({});await userEvent.click(await screen.findByText('Finance'));await screen.findByText('Full path');await userEvent.click(screen.getByRole('button',{name:'Manage Positions'}));expect(await screen.findByText('FUTURE')).toBeInTheDocument();expect(screen.getByText('EXPIRED')).toBeInTheDocument();expect(screen.getByText('VACANT')).toBeInTheDocument();await userEvent.type(screen.getByLabelText('Position name'),'Head');await userEvent.click(screen.getByRole('button',{name:'Add position'}));expect(await screen.findByText(/Configuration error: unique authority/)).toBeInTheDocument();const dialog=screen.getByRole('dialog');await userEvent.click(within(dialog).getAllByRole('button',{name:'Edit'})[0]);expect(within(dialog).getByLabelText('Position name')).toHaveValue('Future');await userEvent.click(within(dialog).getAllByRole('button',{name:'Archive'})[0]);await waitFor(()=>expect(api.archiveOrganizationPosition).toHaveBeenCalledWith(1))});
test('detail shows identity, path and actual authorities',async()=>{setup();await userEvent.click(await screen.findByText('Finance'));expect(await screen.findByText('Institute › Finance')).toBeInTheDocument();expect(screen.getByText('Finance legacy')).toBeInTheDocument();expect(screen.getByText('Grace')).toBeInTheDocument();expect(screen.getAllByText('Ada').length).toBeGreaterThan(0)});