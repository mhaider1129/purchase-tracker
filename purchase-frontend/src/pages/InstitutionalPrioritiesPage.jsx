import React,{useEffect,useState} from 'react'; import {getPublicPriorityQueue} from '../api/procurementPriority'; import PriorityTierBadge from '../components/priority/PriorityTierBadge';
export default function InstitutionalPrioritiesPage(){const [state,setState]=useState({loading:true,data:[],error:null});const [tier,setTier]=useState('');useEffect(()=>{getPublicPriorityQueue().then(x=>setState({loading:false,data:x.data||[],error:null})).catch(e=>setState({loading:false,data:[],error:e.response?.status===503?'unavailable':'error'}));},[]);if(state.loading)return <main className="p-6">Loading procurement priorities…</main>;if(state.error==='unavailable')return <main className="p-6">No priority data configured yet</main>;if(state.error)return <main className="p-6">Unable to load procurement priorities.</main>;const rows=state.data.filter(x=>!tier||x.tier===tier);return <main className="p-6"><h1 className="text-2xl font-bold">Institutional Priorities</h1><label>Tier <select value={tier} onChange={e=>setTier(e.target.value)}><option value="">All</option>{['P0','P1','P2','P3','P4'].map(x=><option key={x}>{x}</option>)}</select></label>{!rows.length?<p>No active procurement priorities</p>:<div className="mt-4 overflow-x-auto"><table className="w-full"><thead><tr><th>Rank</th><th>Priority</th><th>Tier</th><th>Age</th><th>Impact</th><th>Workflow stage</th><th>Members</th></tr></thead><tbody>{rows.map((x,i)=><tr key={`${x.is_group}-${i}`}><td>{x.institutional_rank}</td><td>{x.public_title}</td><td><PriorityTierBadge tier={x.tier}/></td><td>{x.age==null?'—':`${x.age} days`}</td><td>{x.impact||'—'}</td><td>{x.case_status}</td><td>{x.member_count}</td></tr>)}</tbody></table></div>}</main>}