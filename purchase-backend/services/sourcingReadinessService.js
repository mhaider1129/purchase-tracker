'use strict';
const fail = items => Object.assign(new Error('Approved request contains lines requiring item identity resolution'), { code:'ITEM_IDENTITY_RESOLUTION_REQUIRED', status:409, unresolved_item_ids:items.map(i=>i.id), unresolved_item_names:items.map(i=>i.item_name) });
const isReady = item => { const mode=String(item.request_mode||'').toLowerCase(); if(!mode) return true; if(mode==='service') return true; if(mode==='approved_free_text_exception') return item.catalog_status==='approved_exception' && Boolean(String(item.restriction_justification||'').trim()); return Boolean(item.generic_item_id) && item.catalog_status==='catalogued' && !['free_text','pending_item_creation'].includes(mode); };
async function assertRequestItemsReadyForSourcing(db,requestId,providedItems){const items=providedItems||(await db.query(`SELECT id,item_name,request_mode,catalog_status,generic_item_id,restriction_justification FROM requested_items WHERE request_id=$1 AND LOWER(COALESCE(approval_status,'approved'))<>'rejected' ORDER BY id`,[requestId])).rows;const unresolved=items.filter(i=>!isReady(i));if(unresolved.length)throw fail(unresolved);return items;}
module.exports={assertRequestItemsReadyForSourcing,isReady};