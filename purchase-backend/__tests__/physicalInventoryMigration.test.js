const fs=require('fs');const path=require('path');
const sql=fs.readFileSync(path.join(__dirname,'../sql/manual/030_fixed_asset_physical_inventory.sql'),'utf8');
test('migration is manual, transaction wrapped, rerun-safe, and drift-failing',()=>{expect(sql).toMatch(/MANUAL\/PENDING/);expect(sql).toMatch(/BEGIN;/);expect(sql).toMatch(/SQL_030_PARTIAL_OR_DRIFTED_SCHEMA/);expect(sql).toMatch(/SQL_030_ALREADY_APPLIED_COMPATIBLE/);expect(sql).toMatch(/COMMIT;/)});
test('schema freezes snapshots and provides scoped integrity indexes',()=>{expect(sql).toMatch(/prevent_inventory_snapshot_mutation/);expect(sql).toMatch(/BEFORE UPDATE OR DELETE/);expect(sql).toMatch(/UNIQUE\(session_id,dedupe_key\)/);expect(sql).toMatch(/asset_inventory_one_open_finding_uq/);expect(sql).toMatch(/next_asset_inventory_number/);expect(sql).not.toMatch(/MAX\s*\(/i)});
test('all narrow permissions and reconciliation types exist',()=>{for(const p of ['view','create','count','review','resolve'])expect(sql).toContain(`fixed-assets.inventory.${p}`);for(const type of ['VERIFIED','MISSING','UNEXPECTED','WRONG_LOCATION','RESPONSIBILITY_MISMATCH','CUSTODY_MISMATCH','IDENTIFICATION_CONFLICT','CONDITION_MISMATCH','STATUS_MISMATCH','REQUIRES_INVESTIGATION','UNREGISTERED'])expect(sql).toContain(`'${type}'`)});