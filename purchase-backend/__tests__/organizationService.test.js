const {createOrganizationService}=require('../services/organizationService');
function fixture(){const units=[{id:1,name:'Institute',unit_type:'INSTITUTE',parent_unit_id:null},{id:2,name:'CEO',unit_type:'EXECUTIVE_OFFICE',parent_unit_id:1},{id:3,name:'CMO',unit_type:'EXECUTIVE_OFFICE',parent_unit_id:1},{id:4,name:'Nursing',unit_type:'DEPARTMENT',parent_unit_id:2,department_id:10,classification:'Medical'}];const positions=[{id:1,organization_unit_id:2,position_type:'EXECUTIVE_HEAD',position_name:'CEO',user_id:5,user_name:'A',is_active:true,is_unit_head:true},{id:2,organization_unit_id:4,position_type:'DEPARTMENT_HEAD',position_name:'Head of Nursing',user_id:17,user_name:'B',is_active:true,is_unit_head:true}];const ancestors=async id=>{const out=[];let u=units.find(x=>x.id==id);while(u?.parent_unit_id){u=units.find(x=>x.id===u.parent_unit_id);out.push(u)}return out.reverse().map((x,i)=>({...x,depth:out.length-i}))};const descendants=async id=>units.filter(x=>{let u=x;while(u?.parent_unit_id){if(u.parent_unit_id==id)return true;u=units.find(v=>v.id===u.parent_unit_id)}return false});const query=jest.fn(async(sql,args)=>{if(sql.includes('UPDATE organization_units')){const u=units.find(x=>x.id==args[0]);u.parent_unit_id=args[5];u.is_active=args[8]??u.is_active;return{rows:[u]}}if(sql.includes('INSERT INTO organization_units')){const u={id:units.length+1,name:args[1],unit_type:args[3],parent_unit_id:args[4]};units.push(u);return{rows:[u]}}return{rows:[]}});const client={query,release:jest.fn()};return{units,positions,repo:{pool:{connect:async()=>client},get:async id=>units.find(x=>x.id==id),list:async f=>units.filter(x=>f.parent===undefined||x.parent_unit_id==f.parent),ancestors,descendants,positions:async id=>positions.filter(x=>x.organization_unit_id==id)},audit:jest.fn()}};
test('creates root and child units',async()=>{const f=fixture(),s=createOrganizationService(f.repo,f.audit);expect((await s.create({name:'Root',unitType:'INSTITUTE',actorId:1})).name).toBe('Root');expect((await s.create({name:'IT',unitType:'DEPARTMENT',parentUnitId:2,actorId:1})).parent_unit_id).toBe(2)});
test('rejects self parent and circular move',async()=>{const f=fixture(),s=createOrganizationService(f.repo,f.audit);await expect(s.update(2,{parentUnitId:2,actorId:1})).rejects.toThrow('own parent');await expect(s.update(2,{parentUnitId:4,actorId:1})).rejects.toThrow('circular')});
test('resolves ancestors, executive owner and department head',async()=>{const f=fixture(),s=createOrganizationService(f.repo,f.audit);expect((await s.getAncestorUnits(4)).map(x=>x.name)).toEqual(['Institute','CEO']);expect((await s.resolveExecutiveOwner(10)).position).toBe('CEO');expect((await s.resolveDepartmentHead(10)).user_id).toBe(17)});
test('move changes owner but not classification and archive is soft',async()=>{const f=fixture(),s=createOrganizationService(f.repo,f.audit);f.positions.push({organization_unit_id:3,position_type:'EXECUTIVE_HEAD',position_name:'CMO',user_id:6,is_active:true});await s.update(4,{parentUnitId:3,actorId:1});expect((await s.resolveExecutiveOwner(10)).position).toBe('CMO');expect(f.units[3].classification).toBe('Medical');await s.archive(4,1);expect(f.units[3].is_active).toBe(false)});